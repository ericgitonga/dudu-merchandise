-r requirements.txt
pytest==9.1.1
pillow==10.4.0  # scripts/add_catalogue_dimensions.py only — never imported by app.py itself
requests==2.34.2  # scripts/smoke_test_production.py only — never imported by app.py itself
